# 6. 纯标准卷积 NAS 策略 —— 最终修订方案 ⭐

> 返回 [目录](00_README.md)
>
> **核心理念**: 放弃 Block Type 搜索，转向搜索标准卷积的「配置维度」—— **每位置深度（ResBlock 堆叠数）**。
>
> 本章是对 [第 5 篇（头脑风暴）](05_Block_Type_NAS_Brainstorm.md) 策略的全面修订，基于以下关键洞察：
> DW Conv 在 NPU 上的效率极低，使得任何使用 DW 的块都不划算。

---

## 10.1 放弃 DW 块的根本原因

### NPU「每 FLOP 的 FPS 收益」对比

| 算子类型 | NPU 利用率 | 每消耗 1 Cycle 完成的有效 MAC |
|---------|-----------|------------------------------|
| 标准 Conv 1×1 | 90-105% | 0.90-1.05 |
| 标准 Conv 3×3 | 36-100% | 0.36-1.00 |
| **DW Conv 3×3** | **5-9%** | **0.05-0.09** |

这意味着 **标准 Conv 的「每 FLOP 的 FPS 收益」是 DW Conv 的 4-20 倍**。

从精度角度：
- FLOP 数可以粗略估计特征提取能力（更多 MAC → 更强的特征变换能力）
- **标准 Conv**: 每个 FLOP 贡献密集的跨通道交互 → 高精度价值
- **DW Conv**: 每个 FLOP 仅做单通道空间卷积，无跨通道交互 → 低精度价值

### 结论

> **DW Conv 在 NPU 上同时具有「低速度效率」和「低精度效率」。**
> 任何使用 DW Conv 的块（MBConv、ShuffleNet）在 Ethos-U55 NPU 上都不划算。
>
> **正确策略**: 固定使用标准 Conv（ResBlock），搜索「把多少计算量配置在哪里」。

---

## 10.2 搜索维度设计: 每位置子块数量 (Per-Position Depth)

在每个骨干位置，独立堆叠 1~3 个 ResBlock。
ResBlock **不改变通道数**，因此堆叠任意数量都不影响通道流：

```
当前架构 (每位置 1 个 ResBlock):
  enc_0 = [ResBlock] × 1  →  2 层 Conv3×3

搜索后 (每位置可选 1~3 个):
  enc_0 = [ResBlock] × D₀  →  2×D₀ 层 Conv3×3
```

各位置每增加一个 ResBlock 的 **额外 Cycle 成本** (InitNeurons=32):

| 位置 | 分辨率 | 通道 | 单块 Cycle | NPU 利用率 | ×2 总 Cycle | ×3 总 Cycle |
|------|--------|------|-----------|-----------|------------|------------|
| enc_0 | 48×64 | 64ch | 4.40M | 80-93% | 8.80M | 13.20M |
| enc_1 | 24×32 | 128ch | 4.21M | ~90% | 8.42M | 12.63M |
| **dec_0** | 12×16 | 256ch | **9.72M** | **36%** | **19.44M** | **29.16M** |
| dec_1 | 24×32 | 128ch | 4.17M | ~90% | 8.34M | 12.51M |

> **关键发现**:
>
> - **dec_0 性价比最差**: 9.72M/块，NPU 利用率仅 36%（空间分辨率太小 12×16，MAC 阵列利用不充分）→ **限制为 {1, 2}**
> - **enc_0/enc_1 性价比最高**: 4.2-4.4M/块，NPU 利用率 80%+ → **增加深度最划算**
> - **子块数量完全独立**: 每个位置的选择不影响其他位置的通道和连接

### 为什么深度搜索有意义

增加一个 ResBlock = 增加 2 层密集 Conv3×3，直接带来：
- **更大的感受野**: 每多 1 个 ResBlock，感受野扩大 2 个像素（每方向）
- **更深的特征变换**: 更多非线性层 = 更强的抽象能力
- **光流的关键需求**: 大位移需要大感受野，纹理匹配需要强判别特征

### 深度增加的 Cycle 代价（线性增长）

```
加深: enc_0 1→2, enc_1 1→2 (编码器 +2 个 ResBlock)
  骨干: 22.5M → 31.1M (+38%)
  代价: 8.6M extra cycles
  收益: 编码器 4→8 层 Conv3×3，感受野翻倍，特征变换更深
```

> **每位置加深是线性代价（+16-19% per block），仅影响加深位置，其他不变。**
> 对于固定 Cycle 预算，加深是最高效的容量扩展方式。

---

## 10.3 通道流参考（固定 InitNeurons=32）

完整通道流图：

```
输入 (H×W×6)
    │
    ├─ Stem Conv7×7 stride=2:  6ch → 32                   ← H/2 × W/2
    ├─ Stem Conv5×5 stride=2:  32 → 64                    ← H/4 × W/4
    │
    ├─ [enc_0] ResBlock × D₀:  64 → 64  (不改变通道)      ← H/4 × W/4
    ├─ Stride Conv:             64 → 128                   ← H/8 × W/8
    ├─ [enc_1] ResBlock × D₁:  128 → 128  (不改变通道)    ← H/8 × W/8
    ├─ Stride Conv:             128 → 256                  ← H/16 × W/16
    │
    ├─ [dec_0] ResBlock × D₂:  256 → 256  (不改变通道)    ← H/16 × W/16
    ├─ Upsample Conv:           256 → 128                  ← H/8 × W/8
    ├─ [dec_1] ResBlock × D₃:  128 → 128  (不改变通道)    ← H/8 × W/8
    ├─ Upsample Conv:           128 → 64                   ← H/4 × W/4
    │
    ├─ Head0 predict (K₀):     64 → NumOut                 ← H/4 × W/4
    ├─ Head1 upsample (K₁):    64 → 32                    ← H/2 × W/2
    ├─ Head1 predict (K₂):     32 → NumOut                 ← H/2 × W/2
    ├─ Head2 upsample (K₃):    32 → 16                    ← H × W
    ├─ Head2 predict (K₄):     16 → NumOut                 ← H × W
    │
    └─ AccumPreds: Resize + Add                            ← H × W
```

> **ResBlock 保持通道不变**，通道变化只发生在 Stride Conv 和 Upsample Conv 处。
> 无论每位置堆叠多少个 ResBlock，通道流完全不受影响，不存在前后不对应的问题。

---

## 10.4 完整 NAS 搜索空间

### 骨干搜索维度

| 参数 | 含义 | 选项 | 组合数 |
|------|------|------|--------|
| enc_0_depth | enc_0 的 ResBlock 堆叠数 | {1, 2, 3} | 3 |
| enc_1_depth | enc_1 的 ResBlock 堆叠数 | {1, 2, 3} | 3 |
| dec_0_depth | dec_0 的 ResBlock 堆叠数 | {1, 2} | 2 |
| dec_1_depth | dec_1 的 ResBlock 堆叠数 | {1, 2} | 2 |
| **骨干小计** | | | **36** |

### Head 搜索维度（已确定）

| 参数 | 含义 | 选项 | 组合数 |
|------|------|------|--------|
| Head0 output kernel | Head0 输出卷积核 | {3×3, 5×5, 7×7} | 3 |
| Head1 upsample kernel | Head1 上采样卷积核 | {3×3, 5×5} | 2 |
| Head1 output kernel | Head1 输出卷积核 | {3×3, 5×5, 7×7} | 3 |
| Head2 upsample kernel | Head2 上采样卷积核 | {3×3, 5×5, 7×7} | 3 |
| Head2 output kernel | Head2 输出卷积核 | {3×3, 5×5, 7×7} | 3 |
| **Head 小计** | | | **162** |

### 总搜索空间

| 维度 | 组合数 |
|------|--------|
| 骨干 | 36 |
| Head | 162 |
| **总计** | **5,832** |

---

## 10.5 代表性配置 Cycle 预估（192×256, ResBlock, InitNeurons=32）

基于实测 per-layer 数据推算：

| 配置名 | enc depths | dec depths | 骨干 Cycle | 总 Cycle (估) | FPS (估) | 设计意图 |
|--------|-----------|-----------|-----------|-------------|---------|---------|
| **Baseline** | [1, 1] | [1, 1] | 22.5M | 54.1M | 8.4 | 当前基线 |
| **Deep-Enc0** | [2, 1] | [1, 1] | 26.9M | 58.5M | ~6.9 | enc_0 强化 |
| **Deep-Enc** | [2, 2] | [1, 1] | 31.1M | 62.7M | ~6.4 | 编码器全强化 |
| **Deep-All** | [2, 2] | [2, 2] | 45.0M | 76.6M | ~5.2 | 全位置加深 |
| **Max-Enc** | [3, 3] | [1, 1] | 39.7M | 71.3M | ~5.6 | 编码器最大深度 |
| **Shallow-Dec** | [2, 2] | [1, 1] | 31.1M | 62.7M | ~6.4 | 深编码 + 浅解码 |
| **Deep-Dec** | [1, 1] | [2, 2] | 36.4M | 68.0M | ~5.9 | 浅编码 + 深解码 |

> **核心问题**: 编码器加深 vs 解码器加深，哪个对光流精度更重要？
> 这无法通过 Benchmark 回答，**必须通过训练精度验证**。

---

## 10.6 与第 9 章方案的对比

| 维度 | 第 9 章（Block Type 搜索） | 第 10 章（每位置深度搜索） |
|------|--------------------------|------------------------|
| Block 类型 | 5 种 (ResBlock, ShuffleNet, MBConv_E2/E4, FusedMBConv) | **仅 ResBlock** |
| NPU 效率 | 混合（高效标准 Conv + 低效 DW Conv） | **全部高效**（仅标准 Conv） |
| 每 FLOP 的精度价值 | 不均匀（DW FLOP 精度价值低） | **均匀**（每个 FLOP 都是密集通道交互） |
| 搜索内容 | 在哪用什么块 | **在哪配多少深度** |
| 搜索空间 | 54 骨干 × 4 超参 = 216 | **36** 骨干配置 |
| 实现复杂度 | **高**（多种块实现 + NPU 验证） | **低**（仅 ResBlock + 参数化循环） |
| 精度风险 | DW 块可能显著损精度 | **低**（标准 Conv 是光流网络的主流选择） |

> **结论**: 第 10 章方案更简洁、更高效、更可靠，且实现成本更低。

---

## 10.7 推荐的分层搜索策略

### Phase 1: NPU Benchmark 筛选骨干 (36 配置)

1. **固定 Head 为全 3×3**（最快配置，消除 Head 变量）
2. **遍历 36 种骨干配置**: 3 enc_0_depth × 3 enc_1_depth × 2 dec_0_depth × 2 dec_1_depth
3. 每种配置 → 生成 TFLite → Vela 编译 → 记录 FPS、SRAM、总 Cycles
4. 绘制 **FPS vs 参数量** (或 MACs) 的 Pareto 前沿
5. 选出 Pareto 前沿上的 **Top-5 骨干配置**

### Phase 2: NPU Benchmark 筛选 Head (162 配置)

1. 固定骨干为 Phase 1 的 **Top-3** 配置
2. 遍历 162 种 Head kernel 配置
3. 选出每种骨干下 **FPS vs Head 感受野** 的 Pareto 前沿
4. 共得到 3×162 = 486 个数据点 → 选出 **Top-10 完整配置**

### Phase 3: 训练精度验证 (Top-N)

1. 选择 Phase 1+2 的 **Top-10 完整配置**
2. 实际训练并评估 **EPE / Fl-all / KITTI 排名指标**
3. 确定最终最优配置

> **Phase 1 规模很小**: 仅 36 次 Benchmark，每次约数分钟，总计约 2 小时即可完成。

---

## 10.8 代码实现要点

### 网络文件修改

需要新增参数支持每位置独立深度：

```python
class MultiScaleResNet(BaseLayers):
    def __init__(self, ..., EncDepths=None, DecDepths=None, ...):
        # EncDepths = [2, 1]  → enc_0 堆叠 2 个 ResBlock, enc_1 堆叠 1 个
        # DecDepths = [1, 2]  → dec_0 堆叠 1 个 ResBlock, dec_1 堆叠 2 个
        self.EncDepths = EncDepths or [1] * self.NumSubBlocks
        self.DecDepths = DecDepths or [1] * self.NumSubBlocks
```

```python
    def ResNetBlock(self, inputs):
        # Encoder
        for i in range(self.NumSubBlocks):
            for j in range(self.EncDepths[i]):           # ← 新增: 每位置多个子块
                Net = self.ResBlock(Net, NumFilters)
            NumFilters = int(NumFilters * self.ExpansionFactor)
            Net = self.Conv(inputs=Net, filters=NumFilters)  # stride conv

        # Decoder
        for i in range(self.NumSubBlocks):
            for j in range(self.DecDepths[i]):           # ← 新增: 每位置多个子块
                Net = self.ResBlock(Net, NumFilters)
            NumFilters = int(NumFilters / self.ExpansionFactor)
            Net = self._get_upsample(Net, NumFilters, with_bn_relu=False)
```

**核心优点**: ResBlock 不改变通道数，因此无论堆叠多少个，通道流完全不受影响。

### Benchmark 脚本

遍历所有组合：

```python
ENC_DEPTH_OPTIONS = [1, 2, 3]          # enc_0, enc_1 各自可选
DEC_DEPTH_OPTIONS = [1, 2]             # dec_0, dec_1 各自可选

for enc0_d in ENC_DEPTH_OPTIONS:
    for enc1_d in ENC_DEPTH_OPTIONS:
        for dec0_d in DEC_DEPTH_OPTIONS:
            for dec1_d in DEC_DEPTH_OPTIONS:
                config = {
                    'EncDepths': [enc0_d, enc1_d],
                    'DecDepths': [dec0_d, dec1_d],
                }
                # generate_tflite(config) → vela_compile() → record_results()
```

---

## 10.9 实施优先级

| 优先级 | 任务 | 工作量 | 说明 |
|--------|------|-------|------|
| 🥇 P0 | 实现参数化网络文件 | 低 | 在 ResNetBlock 中加入 EncDepths/DecDepths 循环 |
| 🥇 P0 | 实现骨干 Benchmark 脚本 | 中 | 遍历 36 种骨干配置 + Vela 编译 |
| 🥈 P1 | 执行 Phase 1 + 分析 | 低 | ~36 次 Benchmark，绘制 Pareto 前沿 |
| 🥈 P1 | 执行 Phase 2 + 分析 | 中 | ~486 次 Benchmark，确定 Top-10 完整配置 |
| 🥉 P2 | Phase 3 训练验证 | 极高 | Top-10 配置的实际训练 + 光流精度评估 |
